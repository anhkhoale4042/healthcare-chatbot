# Environment settings
ENV=development
LOG_LEVEL=info

# Database (placeholder, in case add one later)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your-username
DB_PASSWORD=your-password
DB_NAME=chatbot_db

# API Keys (add when integrate services like OpenAI or Google)
OPENAI_API_KEY=your-api-key-here
